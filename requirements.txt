# Core application dependencies
Flask==3.1.0
Flask-CORS==5.0.1
Flask-JWT-Extended==4.7.1
Flask-SQLAlchemy==3.1.1
bcrypt==4.2.1
python-dotenv==1.1.0

# Production WSGI server (Windows-compatible; installs everywhere).
# On Linux you may prefer gunicorn instead:  pip install gunicorn
#   FLASK_ENV=production gunicorn -w 4 -b 127.0.0.1:8000 wsgi:app
waitress==3.0.2

# PostgreSQL driver (production database). Omit if you stay on SQLite.
psycopg2-binary==2.9.10
